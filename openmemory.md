# OpenMemory Guide

## Overview

This workspace's OpenMemory MCP **always** uses one VM: GCE `openmemory-uswest`. No Tailscale, no Coolify replica, no sibling clusters.

- **project_id:** `Stackgen_Website_Redesign`
- **USER namespace:** `StackgenWebsite`
- **Only host:** GCE `openmemory-uswest` (`us-west1-b`, tag `openmemory`, public `136.67.59.238`)
- **Containers:** `openmemory-mcp-stackgenwebsite-uswest` (`mem0/openmemory-mcp:latest`, `USER=StackgenWebsite`) + `mem0_store-stackgenwebsite-uswest` (Qdrant, unpublished)
- **MCP port:** `8771` → container `8765`
- **Firewall:** `allow-openmemory-uswest` (`default` VPC, tcp/8771). Allowed source IPs: `12.3.77.187/32` (current) + `12.179.95.29/32` (prior). Update the rule when this workspace's egress IP changes — do not fail over to another VM.
- **Workspace MCP only:** `.cursor/mcp.json` → `openmemory`. Do not change `~/.cursor/mcp.json` (that is Zehra / other workspaces).
- **SSE URL:** `http://136.67.59.238:8771/mcp/cursor/sse/StackgenWebsite`

## Architecture

```
Cursor (this workspace only)
  └─ .cursor/mcp.json → openmemory
       └─ http://136.67.59.238:8771/mcp/cursor/sse/StackgenWebsite
            └─ GCE openmemory-uswest
                 ├─ openmemory-mcp-stackgenwebsite-uswest (USER=StackgenWebsite)
                 └─ mem0_store-stackgenwebsite-uswest
```

## User Defined Namespaces

- [Leave blank - user populates]

## Components

| Component | Location | Purpose |
|---|---|---|
| Project OpenMemory MCP | `.cursor/mcp.json` | Workspace-only Cursor MCP. Always `http://136.67.59.238:8771/mcp/cursor/sse/StackgenWebsite` on GCE `openmemory-uswest`. |
| Project Webflow MCP | `.cursor/mcp.json` | Workspace-scoped remote Webflow MCP for Webflow site/design operations |
| **Webflow Cloud app (`stackgen-web`)** | `web/webflow.json`, `@opennextjs/cloudflare`, `web/package-lock.json` (CLI requires npm) | **2026-08-29:** Standalone Cloud SSR app first-deployed. siteId `6a93a93c010322a33528c733`, app `fdccfeab-a8b1-415b-8b1f-47af5d9df687`, env `52e2d7d4-1c3a-4de7-9c87-187fe7abe5c3`, workspace StackGen Web `6a24cefea342581da3cc81e1`. Live: https://stackgen-web.webflow.io/. Auth in `web/.env` (never commit). Redeploy from `web/` with `--site-id` + `--mount /` + `--environment main`. **2026-08-29:** keep `esbuild` as a direct `devDependency` — `@opennextjs/cloudflare` imports it at build time but does not declare it; `npm ci` (CLI) otherwise fails with `ERR_MODULE_NOT_FOUND`. |
| Project Sybill MCP | `.cursor/mcp.json` | Workspace-scoped Sybill sales-intelligence MCP (calls, deals, accounts) via OAuth |
| Project Figma MCP | `.cursor/mcp.json` | Official remote Figma Dev Mode MCP (design context, Code Connect, canvas write) via OAuth |
| Project Pencil MCP | `.cursor/mcp.json` → `pencil-docker` | Workspace-only Pencil MCP. Launcher `~/.cursor/bin/pencil-mcp-docker-launcher.sh` with `PENCIL_PROJECT_SLUG=stackgen_website_redesign`. Official pen.dev docs auto-register a global `pencil` via the extension (`vscode.cursor.mcp.registerServer`); this workspace disables that and keeps Pencil in project mcp.json only. |
| **Docs site** | `web/app/docs/` (`page.tsx` hub, `[...slug]/page.tsx` articles), `web/content/docs/`, `web/lib/docs/`, `web/components/docs/` | **2026-08-29:** Live corpus imported (417 pages). Dest tree `/docs`, `/docs/stackgen/*`, `/docs/aiden/*`, `/docs/observenow/*`. Reading chrome: nested collapsible folder tree, product switcher + search, breadcrumbs, sticky TOC, replica nav/footer kept. Catch-all is required `[...slug]` (optional catch-all conflicts with the hub). **2026-08-29 href 404s:** `rewriteDocsHref` / `destFromSourcePath` must treat `/docs/{stackgen,aiden,observenow}/*` as already-dest (import rewrites first; a second nest produced `/docs/stackgen/stackgen/...`). Omitted `/docs` source paths such as `/setup/pat` map to StackGen. Static `/cdn` `/assets` stay on docs.stackgen.com. |
| **GKE `stackgen-web` (us-west1-b)** | `k8s/web.yaml`; GCP `propane-galaxy-498403-n8`; cluster `stackgen-web`; image `us-west1-docker.pkg.dev/propane-galaxy-498403-n8/stackgen-web/web:<sha>` | **2026-08-29:** Next.js `web/` on one Docker pod. No prior GKE clusters; Autopilot blocked by `CPUS_ALL_REGIONS` (needs 8, ~3 free). Minimal Standard: 1× `e2-medium`, 30GB pd-balanced, zonal `us-west1-b`. Public HTTP: `http://136.66.137.22/` (no TLS). Live image tag **`d7050da`** (digest `sha256:13ff280f5177e0fd4e894b15c8fb07eecc054fb3f8d3c1c1c926e8da29ae19c2`); also tagged `latest`. Rollouts must use `maxSurge: 0` / `maxUnavailable: 1` — a second pod cannot schedule (`Insufficient cpu`). Node SA `332467315549-compute@developer.gserviceaccount.com` has Artifact Registry Reader. Dockerfile: `pnpm install --frozen-lockfile --ignore-scripts` (pnpm 11 `ERR_PNPM_IGNORED_BUILDS` for esbuild/workerd). `.dockerignore` excludes `.env`. Rebuild: `docker build --platform linux/amd64`, push SHA then `"${IMAGE}:latest"` (brace the tag — unquoted `$IMAGE:latest` can mis-tag). `kubectl set image` to the SHA. Reuse this cluster; do not create a second one. |
| Torbit code graph | `user-torbit` MCP → `orbit mcp serve` (`~/.local/bin/orbit` **v0.105.0**) → `~/.orbit/graph.duckdb` (DuckDB **v1.5.5**) | Fixed 2026-08-21: upgraded Orbit `0.78.0`→`0.105.0`; quarantined corrupt DB to `~/.orbit/broken/`; reindexed main + worktrees. **2026-08-28:** also indexed sibling `/Users/swami/Documents/stackgen-vibe-ux` (`main` @ `0bb9b47`; 1651 files, 28831 defs). Query via `run_sql`; refresh with `index`. |
| **stackgen-vibe-ux local Docker** | `/Users/swami/Documents/stackgen-vibe-ux` (`Dockerfile.local`, `src/iac/docker-compose.local.yaml`) | **2026-08-28:** production `Dockerfile` needs GitHub Packages `npm_token` (`read:packages`). Local image vendors sibling `/Users/swami/Documents/ui-kit` on branch `sagar/all-wip` (matches `@appcd-dev/ui-kit@0.2.11-sagar-all-wip.28`). Run `docker compose -f src/iac/docker-compose.local.yaml up --build` → container `stackgen-vibe-ux`, **http://localhost:3001** (host 3000 is website prod). |
| GCE OpenMemory VM | `openmemory-uswest` (`136.67.59.238:8771`) | Sole OpenMemory host for this workspace. Isolated Qdrant + MCP (`USER=StackgenWebsite`). |
| **Before/After diagram (`a75dC` / `uvKGu`)** | `web/components/sections/home/shelf/BeforeAfter.tsx`, `web/content/home-shelf.ts` | **2026-08-27 animation-ready 4/3-beat rail:** 420px panels. Today: signals → Context lost → Ops teams → Manual approval. Future: signals → Aiden OS (Shared context inside) → Policy gate. `data-animate` hooks on rail nodes. Pencil + React synced. Export: `exports/web-shelf/uvKGu-animation-ready-v4/uvKGu.png`. |
| **Inner→Outer Loop shells (`RBepL` / `k3vas0`+`eYtt6`)** | Pencil `RBepL` → shells `k3vas0` (inner) / `eYtt6` (outer); React `web/components/replica/diagrams/InnerOuterLoop.tsx` + `ParticleField.tsx` | **2026-08-31 pencil-to-code:** Soft Structuralism boxes. **Quieter then rebalance:** Corridor clip kept (no spill past shells). Restored Context Graph join: `corridor-stitch` DrawPath+Beam (clipped), particles `maxParticles=28` + `labels="hub"`, stronger hub lock glow. Shells stay clean; motion lives in the hub band. |
| **OCG rebuild options A–E (comps)** | `scripts/generate-ocg-rebuild-options.mjs` → `.impeccable/mocks/ocg-rebuild-options/` | **2026-08-29 Nano Banana Pro** (`gemini-3-pro-image`) concept plates from V2P0L: A execution spine + real multi-hop graph; B two-plane KG vs decision traces; C hub constellation; D interactive ask→traverse; E three-layer Telemetry / Context Graph / Aiden. Skills: `imagegen-frontend-web` (one image per option). Not live React. |
| **OCG live Option E (signal-drop)** | React `web/components/replica/diagrams/OperationalContextGraph.tsx` | **2026-08-29:** Three-layer stack shipped live — Telemetry (quiet) → Context Graph (typed SVG hub+edges) → Aiden (router/assemblies/OS). Motion `data-motion-metaphor="signal-drop"`: drop rails + nodes→edges→hop beams. Skills: impeccable animate + craft-floor, `design-taste-frontend`, catalog `diagram-animation`. Anti-slop: no ambient glow pulses; ds-* materials. Tests: `diagram-ocg-v2p0l.test.tsx`. **Ask bar:** `data-part=ask-bar` at diagram top (above Telemetry), not nested in Context Graph. |
| **OCG Intent Router hub (C1 complete)** | same + `.impeccable/mocks/ocg-intent-router-complete/` | **2026-08-30:** Finished plate — ask + Intent Router + 4 equal docks; each dock owns 3 **VendorMark** chips on **explicit feeder lines** (vendors→assembly→router). Mapping: N AWS/Terraform/EKS · E GitHub/GitLab/Jira · S Datadog/Prometheus/PagerDuty+checkout-api · W OPA/Slack/Backstage. **Observe polish:** checkout-api chip in S arc, grey bowed feeder. **Neural mesh motion:** ring+cross synapses, bidirectional `Beam` packets on feeders/spokes/synapses, rotating assembly focus (~2.6s), Soft Structuralism accent only on hot path; `data-motion-metaphor=neural-mesh`. Comps C1/C2 via `generate-ocg-intent-router-complete.mjs`. Skills: diagram-animation, design-taste-frontend, high-end-visual-design, impeccable. Shot: `exports/web-shelf/ocg-intent-router-hub.png`. `data-complete=tethered-c1`. **2026-08-30 hub polish:** Intent Router = machined double-bezel (`glow-source` + `glass-hub-shine` + `glass-tile`, label below disc). **2026-08-30 mesh isotropic fix:** fat/thin packets came from `viewBox="0 0 100 100"` + `preserveAspectRatio=none` on landscape stage; remaps x by measured aspect (`useStageAspect`), even dock radii (~14% orbit), uniform stroke 1 + `vector-effect:non-scaling-stroke` on `svg[data-neural=mesh]`, equal packet radii. Inspo: Mobbin n8n/Railway/FLORA thin isotropic edges; Firecrawl graph-packet search. Tests: `diagram-ocg-v2p0l` + `glass-discipline`. |
| **OCG Option E compact fit** | same + Shell `V2P0L` | **2026-08-29 layout/animate pass:** Distilled to Shell-peer height (~533px). **2026-08-30 distill:** Full-width plate (`w-full`, aligns WhoItsFor @1312/1440) broke `aspect-[4/3]` → ~984px stage; replaced with landscape caps `h-[280|320|360]`, `preserveAspectRatio=none`, smaller docks, tighter ask/OS chrome. Shell dropped nested glass plate + `pad-x` 96 → Assemblies padding (`px-4 md:px-16`). Plate ~510px tall. Mobbin: Railway dense canvas, Antimetal mid-peak. Shots: `exports/web-shelf/ocg-compact-align-dark-1440.png`. |
| **Landing density pass** | Assemblies, WhoItsFor, Shell, Logos, Footer, Offerings, InnerOuterLoop, Integrations, ProductSectionShell | **2026-08-29:** Sitewide wasted-space cut — Assemblies `p-24`→`py-10/12`; WhoItsFor dropped `min-h-[143px]`, tighter tiles; Logos/Shell/product shells `py-10–14`; Offerings/Integrations denser pads. Mobbin Better Stack / TheyDo / Linear. Skills: impeccable layout+distill, design-taste-frontend, Mobbin. |
| **Factory diagrams dense v2** | `InnerOuterLoop.tsx`, `Offerings.tsx`, `Integrations.tsx`, `Assemblies.tsx`, `Stagger.tsx` | **2026-08-29–31:** Density/gloss/keystone iterations. **2026-08-31 Offerings Soft Structuralism (impeccable layout):** Style-transfer from `Offerings.tsx`/`F4Jlp` onto Inner→Outer. React: chips `rounded-md border border-border bg-surface-raised px-2 py-1 text-[11px]` (not tall pills); accent rails on INNER/OUTER; plate `glass-specular border p-2.5` full-width; shells `w-72`; quiet hub `132` / `rounded-md size-11` (no neon glow); `data-tile-density=offerings`. Pencil `RBepL` (+ `x2XAG`): pad 10, shells 260, chip cr=6 pad `[4,8]`, accent-rail headers, hub 200, connectors removed. Puzzle + assemble kept in React. Export: `exports/web-shelf/RBepL.png`. |
| **Factory diagram recreate comps v2** | `scripts/generate-factory-diagram-recreate.mjs` → `.impeccable/mocks/factory-diagram-recreate/` | **2026-08-31:** Nano Banana Pro (`gemini-3-pro-image` via gcloud) options A–D from Firecrawl + Mobbin research (Cycle hub-spoke, Railway bus, Linear air puzzle, beehiiv fork). Thesis: left→hub→right; roomy pills; particle beads on beams. Recommend **A** for code; **C** if puzzle stays. Skills: impeccable layout, imagegen-frontend-web. Not live React yet. |
| **Context Graph hub (`m3kuQ`)** | Pencil inside `RBepL` Main Flow | **2026-08-28 radial animated-ready rebuild:** 280×280 hub-and-spoke (Flourish radial + CSS orbit staging). Outer pulse ring, rotate orbit track 18s, hub core pulse 2.8s, 4 satellites (intent/entities/policies/memory) staggered enter, E/W spark dots. Export: `exports/web-shelf/inner-outer-loop-v2/m3kuQ.png`. Inspo notes in `.firecrawl/context-graph-inspo/`. |
| **Offerings Diagram (`F4Jlp`)** | Pencil `C2kYT` → `F4Jlp`; React `web/components/replica/diagrams/Offerings.tsx` | **2026-08-29 craft rebuild:** app cards with icons + subs; Aiden OS double-bezel (Grounded · Governed · Useful); accent column bars. **2026-08-31 AOF Wave 1 T5:** public titles **Aiden for InfraOps / DevOps** (banned: Infrastructure / Automation product titles). **2026-08-29 refine:** neutral `bg-surface` / `bg-surface-raised` materials, DOM substrate band behind the OS chips, tokenized assemble motion, light-safe colors only. Export: `exports/web-shelf/impeccable-diagram-fix/F4Jlp.png`. |
| **Product Mega Menu (`BCszz`)** | `web/components/replica/nav/ProductMegaMenu.tsx`, `web/content/product-mega-menu.ts`, wired in `ReplicaNav` | **2026-08-29:** Pencil open-state catalog → React dropdown. **Alignment fix:** panel `fixed` + viewport-centered (`data-mega-align=nav-shell`) to match nav `max-w-[1200px]` — was `absolute` centered on Products trigger (clipped left column). Banner + 4 columns; Explore → `/product/<slug>`. **2026-08-30:** panel top uses `--nav-top` + `--nav-island-h` so Fluid Island compact height stays aligned; `onOpenChange` pins nav expanded. |
| **Fluid Island Nav** | `web/components/replica/sections/Nav.tsx` | **2026-08-30:** Expand/compact floating pill. **Liquid Glass:** clear/regular + scroll-edge (dark + **light**). Light materials: frosted white + hairline `rgb(0 0 0 / 0.08)` + layered float shadow (Apple HIG Materials / Mail Photos); denser regular when scrolled; light scroll-edge veil; nested theme chip frost. Nav CTA nested `h-8` glassProminent. Research: `.firecrawl/apple-hig-materials.md`. Shots: `exports/web-shelf/nav-liquid-glass-light-*.png`. **2026-08-31 scroll-down minimize:** Density via `nextNavScrolled` (`lib/nav-scroll-density.ts`) — Apple `tabBarMinimizeBehavior(.onScrollDown)`: compact on scroll-down past 72px, expand on scroll-up / ≤28px top; dir delta 6px hysteresis; rAF scroll. Morph `DUR.navMorph` 420ms (was glassFade 240ms). Island `w-auto max-w-6xl` (dropped flex-1 canyon). Hover-expand leave settle 180ms. Material still hero IO. `data-nav-minimize=on-scroll-down`. Firecrawl: Adopting Liquid Glass. Skills: impeccable animate, navigation-menus, systematic-debugging. |
| **Who It's For (`ck4Dy`)** | Pencil `ck4Dy`; React `WhoItsFor.tsx` | **2026-08-29 layout fix:** Two aligned 4-col rows (pillars with centered labels above cards, then roles) + OS band — not masonry `col-span`. Measured 303px×4 + 12px gap @ 1440 matches Pencil. **2026-08-29 Option B live:** Product portraits (real demo UI frames) + role dock + Aiden OS rail. Mobbin: Better Stack / Deel. Firecrawl CLI used (MCP down). `data-who-layout=portraits-dock`. Shot: `exports/web-shelf/who-its-for-option-b.png`. **Aspect fix:** portrait wells `aspect-video` + `object-contain` (not `aspect-[4/3]`+cover) — same meet rule as Aiden OS left-frame. **2026-08-30 quieter:** Dropped `glass-specular` from portrait cards (sheen washed Observability/SRE stills); whisper top rim only. Gloss stays on role dock. **2026-08-30 bolder light liquid:** Roles + Aiden OS both `glass-specular` plates with nested `glass-tile` chips (was matte `bg-bg` / flat `--ds-layer-os-*`). Light-only dock CSS boost + `ground-who` atmosphere opacity bump. Research: Firecrawl HIG Materials / UX Pilot light boundaries; Mobbin Apple Mail/Notes trays, Air glass card. No content blur. Verify on `:3010` (Docker `:3000` is stale). Shot: `exports/web-shelf/who-docks-liquid-gloss-light-1440.png`. |
| **Footer (`Wp1Dh`)** | React `Footer.tsx` | **2026-08-29 layout refine:** Double-bezel CTA; brand left + tight Product/Platform/Company cluster (no `flex-1` column stretch); Vercel-style status (`bg-pass`); Attio meta bar + Runway bordered socials (Phosphor). Product links → `/product/<slug>`. Mobbin: Attio, Linear, Vercel, Relume, Chronicle. Skills: impeccable layout + craft-floor, design-taste-frontend, high-end-visual-design. Shots: `exports/web-shelf/footer-after.png`. |
| **Home Light shelf (`LexRf`)** | Pencil `LexRf` @ x=92000 (left of `CYfSl`) | **2026-08-29:** Exact structural clone of dark `CYfSl` (8 sections, 523 nodes). `theme.mode=light` + `$ds-*` tokens; Submit on-accent remapped from hardcoded `#000000`. Export: `exports/web-shelf/cyfsl-light-lexrf-v1/LexRf.png`. |
| **Integrations (`hG9Ou` / `XFV0R`)** | Pencil Assemblies → Integrations; React `web/components/replica/diagrams/Integrations.tsx` | **2026-08-29:** Infinite L→R marquee (duplicated track, edge fades, pause-on-hover, 36s). Mobbin: 1Password/Neon/Rox/Headspace. Reduced motion = static row. `data-marquee`. **2026-08-29 layout:** vendor marks `size-8` (32px, 67% of chip) in `h-12` pills (`bg-bg`, `px-4`, `gap-2.5`, `text-sm`); chip gap `gap-4`. Was `h-4` (16px) in `py-2` chips. Inspo: Slack Apps list icons + Phantom icon+name chips. |
| **CYfSl content + primitives (Task 3)** | `web/content/cyfsl.ts`, `web/components/cyfsl/primitives/`, `web/components/cyfsl/*.tsx` | **2026-08-29:** Added `cyfslContent` with the exact eight keys (`nav`, `hero`, `video`, `logos`, `assemblies`, `shell`, `whoItsFor`, `footer`) using canvas copy, plus theme-free `CyfslLogo` and `CyfslPrimaryPill`. Nav, hero, video, logos, footer, and assemblies now render real content; shell and who-its-for remain structural stubs for later tasks. |
| **App Replica Nav + primitives (T2)** | `web/` (worktree `app-replica-next`) | `lib/nav.ts` + `lib/products.ts`; primitives `Logo` (Pencil `mF7lt`), `ButtonPrimary`, `ButtonGhost`, `MonoLabel`, `SectionShell`; layout `Nav` (60px, replica pad, ThemeToggle in actions) + `Footer({ columns })`; nav smoke test. |
| **App Replica product pages (T5)** | `web/` (worktree `app-replica-next`) | `content/products.ts` (4 slugs, verbatim Pencil copy); sections `components/sections/product/*` (Hero, Metrics, KeyCapabilities, Mechanism, EarlyAccessStrip, Testimonial, FinalCta); `app/product/[slug]/page.tsx` with `await params` + `notFound()`; media remapped to `public/media/aiden-*`; EarlyAccessStrip conditional on Infrastructure only. |
| **App Replica Enterprise + Pricing + News (T7)** | `web/` (worktree `app-replica-next`) | `content/{enterprise,pricing,news}.ts` from frames `o8Fqkk`/`V8R69l`/`o303yj`; sections under `components/sections/{enterprise,pricing,news}/`; routes `/enterprise`, `/pricing`, `/news`; Pricing Public Web Rules + FAQ canvas-exact; News Placeholder Items unchanged. |
| **Next.js web app (Wave 1 T1)** | `web/` | Next.js 16.3 standalone app: canvas tokens in `app/globals.css`, typed contracts `lib/types.ts` + `lib/nav.ts`, inert `MotionProvider`/`Reveal`, Vitest, Docker multi-stage + compose profiles (`dev`/`prod`) |
| **Dual-theme home replica (Waves 0–4)** | `web/components/replica/**`, `web/content/replica.ts`, `web/lib/replica-frames.ts` | **2026-08-29:** Live Pencil `CYfSl`/`LexRf` on `/` with theme toggle (`stackgen-theme`). `HomeReplica` composes 8 sections; Docker prod `:3000`. **2026-08-31 AOF Wave 1 T3:** Factory content contract in `replica.ts` — hero H1 "Outcomes, not agents.", Schedule demo + `#how-it-works`, ops-lag problem/solution spine, assemblies Intent→Spec→Runtime→Learning, SHARED WORLD MODEL shell, Offerings pillars InfraOps/DevOps/Observability/SRE with product slugs, Factory footer CTA. Supersedes Approach C hero copy. provisional `REPLICA_FRAMES.*.problem` = `homeProbD`/`homeProbL`; target order Hero→Logos→Problem→Solution→Assemblies (Tasks 2–4). `video.label` removed — demo copy lives on `solution.*`. **Task 2:** `ReplicaProblem` in `sections/Problem.tsx` — mounted after Logos; `id="problem"`, Soft Structuralism symptom list, `AtmosphereField slot="ground-shell"`. **Task 3:** `ReplicaSolution` in `sections/Solution.tsx` — absorbs former `Video.tsx` media plate; `data-pencil-id` on plate uses `REPLICA_FRAMES.*.video`; `Video.tsx` deleted; mount after Problem. **Task 4:** `ReplicaAssemblies` `id="how-it-works"` (Hero secondary anchor); title chrome from `replicaContent.assemblies` (eyebrow/heading/body); h2/link tokens match Problem/Solution. **Task 5:** Governance ban flip — public web allows InfraOps/DevOps/AOF; bans superseded Infrastructure/Automation product titles + ADF; diagram strings + `layout.tsx` metadata updated; vitest in `diagram-offerings`, `diagram-ocg-v2p0l`, `products-placeholder`. |
| **Product pages placeholder (Harness-shaped)** | `web/lib/products.ts`, `web/content/products.ts`, `web/content/product-mega-menu.ts`, `web/components/replica/ProductPage.tsx`, `web/components/replica/product/*`, `web/app/product/[slug]/page.tsx` | **2026-08-29:** Four routes with Harness section order, PLACEHOLDER copy + mono badge stubs, pencil frame ids qwI1S/llzpJ/JQkAE/TIh4G; mega-menu content for `ProductMegaMenu`; vitest `products-placeholder.test.tsx`. **2026-08-31 AOF Wave 1 T2:** Slugs `aiden-for-infraops` / `aiden-for-devops` (+ observability/sre unchanged); titles InfraOps/DevOps; permanent redirects from `/product/aiden-for-infrastructure` and `/product/aiden-for-automation` in `next.config.ts`; hero PNGs renamed under `public/media/product/`. |
| **Atmosphere field (Task 1 polish)** | `web/lib/atmosphere.ts`, `web/components/replica/shared/AtmosphereField.tsx`, `web/__tests__/atmosphere-field.test.tsx` | **2026-08-29:** Slot/theme path helpers (`atmosphereSrc`), per-slot opacity tokens, decorative `AtmosphereField` with `srcOverride={null}` CSS-only fallback and `onError` img hide. PNG assets at `/media/atmosphere/{slot}-{theme}.png` — wired in later tasks. |
| **Impeccable critique → clarify/harden/quieter** | `web/content/replica.ts`, `ThemeProvider`, `theme-init.js`, `ContextGraph`, `LayerRail`, `globals.css` glow | **2026-08-29:** Critique `17/28` then fixes: ADF H1 + Schedule demo vocabulary; theme default dark + post-hydration re-apply; rail outside `role=img`; rail index contrast; light glow 10%. Comps: `.impeccable/mocks/diagram-concepts/`. Audit note: `.impeccable/audit/2026-08-29-home-replica-post-fix.md`. |
| **CYfSl dark-only scaffold (Task 1)** | `web/lib/cyfsl-frames.ts`, `web/app/globals.css`, `web/app/layout.tsx` | Superseded by Wave 0 dual-theme replica foundation. |
| **Primitive components (Wave 1 T2)** | `web/components/primitives/` | React port of canvas shelf: `Logo`, `ButtonPrimary`, `ButtonGhost`, `MonoLabel`, `MetricCell`, `SectionHeaderSplit`, `Nav` (client), `Footer` (server). Token-mapped Tailwind; Logo SVG from `JJx7F` path data; Nav consumes `NAV_ITEMS`/`PRIMARY_CTA`; MetricCell renders mechanism line |
| **Factory structural primitives (Task 4)** | `web/components/primitives/` | Added `NumberedSequence`, `StatBand`, `IridescentBand`, `Marquee`, and `PromptLine` for the factory-anchored home/product sections. `Marquee` uses the shared `useReducedMotion` hook and CSS keyframes in `web/app/globals.css`; `PromptLine` confirms copy state in text and reserves status width to avoid layout shift |
| **Content modules (Wave 1 T3)** | `web/content/` | Typed default-export objects per Wave 1 page (`home`, four `product-*`, `platform`, three case modules, `schedule-demo`, `shared`). Consumes `@/lib/types`; greytHR quote is sole `published` with `sourceUrl`; governance vitest (13 tests) bans Olly/InfraOps/DevOps/em-dash/single-pane-of-glass across all modules; quote/metric checks on all modules except `shared` logos |
| **Routes + section stubs (Wave 1 T5)** | `web/app/**/page.tsx`, `web/components/sections/**`, `web/components/diagrams/**`, `web/components/ComingSoon.tsx` | 17 live routes + 10 ComingSoon; Home composes 12 sections + Footer (intentional canvas deviation); product `[slug]` uses `await params`; remaining `data-stub` sections for parallel fill |
| **Home Hero + Final CTA (Wave 1 T6)** | `web/components/sections/home/Hero.tsx`, `FinalCta.tsx`, `__tests__/Hero.test.tsx`, `FinalCta.test.tsx` | Canvas `XPc1X`/`ZHuzU` via Pencil MCP; content props; `ButtonPrimary` + `Reveal`; no `data-stub`; 6 tests pass |
| **In Their Words + Compliance (Wave 1 T9)** | `web/components/sections/home/InTheirWords.tsx`, `Compliance.tsx` | Quote cards: semantic `<blockquote>` + `<cite>`, visible `PLACEHOLDER` mono badge on unapproved quotes, published greytHR links Source; compliance renders `home.compliance.badges` only (SOC 2/PCI/HIPAA); both use `<Reveal>` |
| `StackGen Logo` (`JJx7F`) | `Stack_Linear.pen` shelf | Official wordmark + gradient icon; ref everywhere |
| `Nav Desktop` (`N6udS`) | Home + reusable | 1440 nav: logo, links, Login, Schedule demo |
| `Btn Primary` (`xcXXD`) | Component shelf | Accent CTA pill; override Label |
| `Btn Ghost` (`NCL0m`) | Component shelf | Hairline outline button |
| `Mono Label` (`e9nTl`) | Component shelf | JetBrains Mono tertiary section label |
| `Metric Cell` (`m2UJ8`) | Component shelf | Big figure + caption |
| `Section Header Split` (`GZSQR`) | Component shelf | Linear H2-left / body-right |
| `Footer Simple` (`igbym`) | Component shelf | Logo + Product/Platform/Company link stub |
| `Direction Contract` (`Vrb0t`) | `Stack_Linear.pen` x=-620 | Locked Linear visual world + copy rules |
| `StackGen Home` (`JLg8h`) | `Stack_Linear.pen` x=960 | Desktop home: Nav→Hero→…→Aiden Family→Compliance→Integrations→In Their Words→Operating Model |
| `StackGen Home — Mobile` (`A38GWG`) | `Stack_Linear.pen` x=2560 | Rebuilt mobile mirror with simplified Home sections and footer ref |
| `Platform` (`HL34b`) | `Stack_Linear.pen` x=4550 | Desktop platform hub: Hero → Two Planes → full Operational Context Graph → Aiden OS + roadmap → product links → final CTA |
| `Creation Ops Diagram` (`OVyjM`) | `Stack_Linear.pen` → `JLg8h` / `vU48B` | Home Problem editable comparison of software creation vs software operations, with gap marker and three failure-mode rows |
| `Factory Process` (`pnlIy`) | `Stack_Linear.pen` → `JLg8h` | Home section between Problem and ADF Loop with four-step Intent → Factory Spec → Factory Runtime → Factory Learning flow |
| **Factory Process diagram (Wave 1 T11)** | `web/components/diagrams/FactoryProcessDiagram.tsx`, `web/components/sections/home/FactoryProcess.tsx` | Live SVG from `factory-process.json`: 4 step cards + 3 connector paths, geometry-normalized Y, CSS var fills, `data-part` motion hooks, content-driven labels |
| **Problem diagram (Wave 1 T10)** | `web/components/diagrams/ProblemDiagram.tsx`, `web/components/sections/home/Problem.tsx` | Live SVG from `problem.json`: creation/ops card, gap marker, 3 failure rows, 3 evidence stats, pull line; Y normalized via `Y0=1037`; cropped viewBox `0 0 1440 1083`; citations prop overrides evidence copy |
| **`DiagramText` primitive (parity pass)** | `web/components/diagrams/DiagramText.tsx` | Width-aware SVG text: `wrapText`/`measureText` split copy into `<tspan>` lines using per-character Inter advance approximations (no DOM, so SSR and client agree). Every diagram body string must pass the canvas box `width`; unwrapped `<text>` was the single cause of all 13 text-overlap defects |
| **Aiden OS diagram (Wave 3 T10)** | `web/components/diagrams/AidenOsDiagram.tsx`, `web/components/sections/platform/AidenOsLinks.tsx`, `web/public/diagram-assets/aiden-os-center.svg` | Figma `1:8708` port for the platform Aiden OS band: one accessible SVG on a dark panel, exact `1920x1080` deck frame viewBox, five capability cards, `DiagramText` width/maxLine bounds, and a compatibility wrapper in `AidenOsLinksDiagram.tsx` while the owned section now imports `AidenOsDiagram` directly |
| **`FinalCtaCompact` (parity pass)** | `web/components/sections/FinalCtaCompact.tsx` | The 272px closing band shared by product, platform and case routes (canvas `v47e5`/`q8grMq`/`qIKEH`/`o0WWaf`/`yhFJB`): 80 top, 44 heading, 20 gap, 32 CTA, 96 bottom, **no body paragraph**. `ProductFinalCta`/`PlatformFinalCta` are thin wrappers; case pages call it directly. Home keeps the taller `ZHuzU` band in `home/FinalCta` |
| **Logos strip (parity pass)** | `web/components/sections/home/Logos.tsx`, `web/content/shared.ts` | Canvas `d751F`: eight **text wordmarks** at 15px/600 `--color-text-tertiary`, a credentials line, and a note line naming the rest. `CUSTOMER_WORDMARKS` replaced the old `CUSTOMER_LOGOS` image list (dark brand SVGs were invisible on `#08090A`) |
| **Integrations grid (parity pass)** | `web/components/sections/home/Integrations.tsx`, `home.integrationCategories` | Canvas `K1zfG` 1278px: 52px split header (heading left / deck right) + seven categories, each a 12px label and 18px subtitle over 52px bordered tiles carrying plain text tool names (35 tools). Replaced the 8 monochrome icon tiles, which rendered black-on-black |
| `Exact Logo Shelf` (`u6p5hC`) | `Stack_Linear.pen` x=12670, y=-2700 | Eight official-brand 48x48 logo components from Simple Icons path data: AWS `j8jzF`, Azure `HoxfJ`, Google Cloud `n2DyG`, Kubernetes `SgvCA`, Docker `PQOV4`, Terraform `l2zNQN`, Git `USeuP`, SonarQube `qp4rE` |
| **Stitch diagram prompt pack** | `.stitch-preview/DESIGN.md`, `.stitch-preview/diagram-prompt-template.md` | **2026-08-31:** Google Stitch SoT for landing diagrams. Live `$ds-*` Soft Structuralism (Geist, dual theme, double-bezel, specular glass, violet cap). Supersedes `.stitch-preview/website-design-system.md` Linear-console archive. Skills: stitch-design-taste, design-taste-frontend, high-end-visual-design, imagegen-frontend-web, diagram-animation, prompt-engineering. Stitch MCP was down this session; prompts are paste-ready for labs.google/stitch. |

## Patterns

- **Stitch diagrams must use live replica tokens, not the Linear archive (2026-08-31):** Dark `#0B0C0E/#151619/#1D1F24/#8C85FF` + Geist; light twin. Violet only on hub or one hot path. One plate per generate. Completeness: unclipped chips, feeders into parents, ask bar above OCG. Concept only — production stays React.
- **Approach C homepage order + v2 H1 (2026-08-31, implemented):** Replica `/` order Nav→Hero→Logos→Problem→Solution→Assemblies→Shell→WhoItsFor→Footer. Hero H1 **"Take control of production change"** + See/Decide/Change pillars in `web/content/replica.ts`; ADF below fold in Assemblies. Design spec: `docs/superpowers/specs/2026-08-31-homepage-content-rewrite-design.md`.
- **Problem A+B chaos film (2026-08-31):** Verbose symptom list replaced by cinema plate + animated siloed fragments (`ProblemChaosFilm.tsx`). Comp: `.impeccable/mocks/problem-animated-explainer/option-AB-hybrid.png`.
- **AOF Ops Lag Pencil (2026-08-31):** Gemini Soft Structuralism comps `soft-A/B/C` via Vertex `gemini-3-pro-image` (`scripts/generate-aof-ops-lag-soft-structuralism.mjs`); inventory `.impeccable/mocks/aof-ops-lag/LAYER-INVENTORY.md` (pick **soft-B-density**). Canvas SoT frames `IOgtr`/`TUuGp`/`jkPzy`. Editable Soft Structuralism redraw `ifJjx` (double-bezel, motion-named layers). Export `ifJjx.png`. **Parked during Wave 1 content ship** — resume: approve `ifJjx` vs soft-B → remaining Pencil queue → React ports. Live Problem still uses `ProblemChaosFilm`.
- **Homepage Problem→Solution→Product (2026-08-31):** Spec `docs/superpowers/specs/2026-08-31-sre-problem-solution-product-narrative.md`. Sourcegraph spine (problem labeled → asymmetric agent demo → verb pillars). Recommended Option A See/Decide/Change; hybrid with Option C SRE symptoms. Replica today leads with ADF/product (`replica.ts`) — insert Problem + Solution before Assemblies/WhoItsFor. Public names: Infrastructure / Automation / Observability / SRE (not PRFAQ DevOps/InfraOps).
- **OpenMemory MCP tool contract (2026-08-29 fix):** GCE bridge exposes **minimal** tools only — `search_memory({ query })`, `add_memories({ text })`, `list_memories({})`, `delete_all_memories({})`. Project scoping is in the SSE path (`StackgenWebsite`), not tool args. Passing `project_id`, `title`, `content`, or `metadata` causes MCP `-32602 Invalid request parameters`. Encode memory type/title/body inside the `text` string (`[component] …`, `[user_preference:global] …`). Launcher `~/.cursor/bin/openmemory-mcp-docker-launcher.sh` pins `--transport sse-only` (skips http-first 405 delay). Health: `~/.cursor/bin/stackgen-mcp-health.sh`. After launcher changes, reload MCP in Cursor Settings. `/` `JLg8h` 9296 · `/product/aiden-for-infrastructure` `T4FJtW` 2685 · `/product/aiden-for-automation` `zTOam` 2478 · `/product/aiden-for-observability` `OAfMk` 1984 · `/product/aiden-for-sre` `bEaQH` 2346 · `/platform` `HL34b` 3338 · `/case-studies` `k1XEU` 1236 · `/case-studies/greythr` `gYoDZ` 1163 · `/case-studies/innovaccer` `YEXx8` 1026 · `/schedule-demo` `K6I26T` 816. The Wave 5 parity audit (`.superpowers/sdd/wave1-parity-report.md`) used a mapping **shifted by one frame**, which invalidated every product/platform finding and produced phantom defects such as "home hero missing dashboard" (canvas `XPc1X` is copy + one CTA, nothing else). Always re-derive from this table. Copy lives in `design-reference/README.md`.
- **Canvas-to-SVG transcription rules (parity pass, 2026-08-20):** (1) Canvas text nodes carry a box `width` **and** a `height` that encodes line count — a `256x40` box at 13px is two wrapped lines. SVG `<text>` never wraps, so always route body copy through `DiagramText` with that width. (2) Canvas diagram frames reserve their own heading/body band; when the React section renders a DOM `SectionHeaderSplit`, crop that band out of the `viewBox` or the page ships ~200-300px of empty SVG per section. (3) Page frames lay sections out **flush** (child heights sum exactly to the frame height), so section wrappers must not add their own vertical padding on top of the canvas band height. (4) Child text nodes often carry an unresolved `y` (a constant like `-4270`); only container frames hold the real offset — deriving a position from a text node's `y` silently yields 0 (this put the Agentic OS card CTA on top of its title).
- **Figma overflow fallback for dense diagram nodes:** when `get_design_context` returns sparse metadata for a full slide, split into smaller sublayers and combine their exact bounds/code. Task 10 `AidenOsDiagram` used `1:13676` for the header band, `1:13681` for the five-card grid, and a downloaded asset from `1:13616` for the center ring while keeping the top-level frame viewBox at `1920x1080`.
- **Monochrome brand assets are unusable on the dark ground:** Simple Icons SVGs ship with no `fill` (default black) and several customer brand SVGs are near-black (`#101010`, `#0E0D0F`, `#002041`). On `--color-bg-base` `#08090A` they load fine but are invisible. The canvas never uses brand artwork for these surfaces — both the logo strip and the integrations grid are text wordmarks at `#7E838C`. Prefer the canvas treatment over tinting third-party marks.
- **Parity diagnostic harness:** `web/e2e/diagnose.mjs` (node, needs the dev server on :3000) reports per-route page height, overlapping SVG `<text>` pairs, text overflowing its own frame, horizontal scroll, and logos sitting on a low-luminance background. Run it after any diagram or section change; the target is `overlaps 0 overflow 0`.
- **Deck visual refinement Tier 1 (2026-08-20):** Defect repair on 15 slide frames per `docs/superpowers/specs/2026-08-20-deck-visual-refinement-design.md` and plan `docs/superpowers/plans/2026-08-20-deck-visual-refinement.md`. Pencil MCP only; read-only frames per plan Constraint 2. Artifacts: `.superpowers/sdd/refine-artifacts/deck-{N}.png`, reports `.superpowers/sdd/refine-task-{T}-report.md`. Deck 4 `yHYky` Task 1.4 complete: deleted 61 noise/stray nodes, timeline cards 560×335 (4-col spans), bottom row 4×414px (3-col spans).
- **Figma literal rebuild grid (2026-08-20):** 15 placeholder frames at grid origin x=10700 y=-1420 (Task 0). Deck 2 `V7m4i` at x=4240 y=10400 rebuilt from Figma `1:14797` (Manual delivery human toll; cream bg, dark right bar panel). Deck 3 `hwbT4` at x=17060 y=-1420 rebuilt from Figma `1:7661` (On-Call Crisis). Deck 13 Roadmap `M8kNAX` at x=12820 y=2420 rebuilt from Figma `1:13969` (three-column Available / Early Access / H1 2027; naming map applied). Deck 15 Diagram `HKug4` at x=17060 y=2420 rebuilt from cached Figma `1:14278` (Control Room hub, five-column pipeline, Platform & Tools band; Figma-only slide, not in PDF). Supersedes old shelf frame `K887J`. Artifacts: `.superpowers/sdd/figma-rebuild-artifacts/deck-{N}.png`. Per-task reports: `.superpowers/sdd/figma-rebuild-task-{T}-report.md` or `.superpowers/sdd/task-{N}-report.md`. Frame names use hyphen not em dash for ban-scan compliance. Copy+Delete workaround required after in-frame build (placeholder `lOMtC` → final `V7m4i`).
- **Exact replica shelf status (2026-08-20 02:14, superseded):** 12 of 14 slide frames built on the `x=12670` column. Done: deck 0 `fF5sz` (y5800), deck 1 `UdNdR` (y2200), deck 2 `yZJZb` (y7000), deck 3 `K887J` (y8200), deck 4 `ekA26` (y9400), deck 5 `YMipV` (y3400), deck 6 `Hp0Tk` (y10600), deck 7 `xdCwb` (y-1420), deck 8 `Y5oI04` (y1000), deck 9 `aPptF` (y4600), deck 10 `k9h3r5` (y11800), deck 12 `PBBa1` (y-220). **Remaining:** deck 11 Aiden OS at y13000, deck 13 Roadmap at y14200, then a design-system alignment pass and final batched verification. Deck 14 is permanently excluded (uncleared modeled dollar ROI).
- **Official brand logo components (2026-08-20):** shelf `u6p5hC` at x=12670 y=-2700 holds eight `reusable` components built from official Simple Icons path data (verbatim `d`, `viewBox [0,0,24,24]`), cached at `.firecrawl/logos/`. Ids: AWS `j8jzF`, Azure `HoxfJ`, Google Cloud `n2DyG`, Kubernetes `SgvCA`, Docker `PQOV4`, Terraform `l2zNQN`, Git `USeuP`, SonarQube `qp4rE`. Inner path ids for per-instance recolour: `sGDYG`, `BFOVp`, `jd1VD`, `YQ5wT`, `ziqAL`, `CKvNa`, `qMPdm`, `yzHLf`. AWS official hex is dark navy, so override its inner path to white on dark grounds. Instance these; never hand-draw a third-party mark. Full notes: `.superpowers/sdd/logo-components-report.md`.
- **Exact shelf design system:** Inter only, no monospace. Left margin 60, content within x 60..1860. Title 44/600/1.1 at ~x60 y52. Framing sentence 15/1.55. Section heading 19-20/600. Card heading 16-18/600. Body 14-15/1.55-1.65. Card sub-copy 11-13/1.35-1.45. Cards radius 4-8, 1px stroke, padding ~[16,20], vertical layout gap 6-10. Accent `#8B5CF6` / `#A78BFA` on dark; teal `#5EEAD4` only where the source uses it. Muted `#9A938C` on dark, `#5B5148` on cream. Ground colour is sampled per slide from the source PNG (some slides are near-black, some cream).
- **Exact diagram replica shelf (2026-08-20):** Separate editable Pencil frames matching `sg-new-postn-deck` composition (not marketing-module paraphrases). Hires SoT: `.firecrawl/postn-deck/hires/deck-*-1.png` @ 300 DPI. Column ~x=12670. Frames: OCG `xdCwb`, Factory Process `PBBa1`, SRE `Y5oI04` (rebuilt, replaced `mtr8t`), Creation/Ops `UdNdR`, Factory Intent `YMipV`, Automation `aPptF` (rebuilt, replaced `dkUBy`). Audit: `.superpowers/sdd/audit/replica-audit-2026-08-20.md`. Build rules learned: use frame layout/gap/padding instead of absolute stacked text; never hand-insert `\n` in body copy; keep stage blocks wide enough that no single word exceeds the text box; Pencil does not rasterise newly created nodes until a later call, and an in-place delete-then-rebuild leaves a frame permanently blank (delete the frame and create a fresh one). Exports under `.superpowers/sdd/replica-artifacts/exact-deck-*`. Do not modify existing Home/Product/Platform while iterating Exact frames. Deck cyan accents allowed on Exact shelf for visual match; site pages stay Linear `#9437FF`.
- Product marketing source of truth: `.agents/product-marketing.md` (v1, 2026-08-19). All copy/IA work reads this first. Primary CTA: Schedule demo only.
- Factory experience brief (IA/craft quality bar): `docs/superpowers/specs/2026-08-19-factory-experience-brief.md`. Steal section spine + density; keep Linear visual world + Schedule demo CTA.
- Positioning + ICP: `docs/superpowers/specs/2026-08-19-positioning-icp.md` (locked from intake).
- Site IA + page briefs: `docs/superpowers/specs/2026-08-19-site-ia-page-briefs.md` (Home P0 + Product/Platform/Cases P1).
- Execution plan: `docs/superpowers/plans/2026-08-19-stackgen-adf-canvas-execution.md`. **Phase 5 Scope A complete** on `Stack_Linear.pen`: Home P0 + Products + Platform hub/children + Cases + About/Schedule Demo/MCP/Pricing/Contact. Wave 2 verify cleared banned terms (Olly, en-dashes, estate).
- **Wave B stubs (2026-08-19)** @ x≈9110: Solutions `uPzvZ`, Motion A `socHN`, Motion B `wTrxq`, Motion C `Bp1dp`, Partners `ARO2U`. Company polish: MCP path cards, Pricing model (no dollars), Contact form stub.
- **Postn deck incorporation (design):** `docs/superpowers/specs/2026-08-19-postn-deck-incorporation-design.md` (Approved). Plan: `docs/superpowers/plans/2026-08-19-postn-deck-incorporation.md`. Source `sg-new-postn-deck/` + `.firecrawl/postn-deck/`. Hybrid C + Linear editable diagrams; ROI $ off public; waves W0 docs → W1 Home → W2 Products → W3 Platform → W4 verify.
- **Postn deck incorporation execution (complete, 2026-08-19):** W1 section ids — Home desktop Problem `vU48B`, Factory Process `pnlIy`, ADF Loop `t5DPzG`, OCG teaser `RBIMd`; Home mobile rekeyed to `A38GWG` (replacing `KUYi6`) with Problem `N50R8e`, Factory Process `REQFZ`, ADF Loop `oU6SE`, OCG teaser `EuDQL`. W2 mechanism ids — Infrastructure `w8Wb0v`, Automation `u2Jbr`, Observability `y3RwQr`, SRE `k3S5j`. W3 Platform rekeyed to `HL34b` (replacing `BQ6Ld`) with full OCG `O4fic7` and Aiden OS + Roadmap `gWRK3`. Plan status: W0–W4 complete; verification report at `.superpowers/sdd/task-12-report.md`.
- **Diagram replica shelf (R0 started, 2026-08-20):** New shelf on `Stack_Linear.pen` at x=10670, y=-1420. Context label `Q15Jfw`. Site marketing frames read-only. Plan: `docs/superpowers/plans/2026-08-19-postn-diagram-replica-shelf.md`. Artifacts: `.superpowers/sdd/replica-artifacts/`.
- **Exact Deck 12 replica (2026-08-20):** Added root frame `PBBa1` named `Exact - Deck 12 - Factory Process` on `Stack_Linear.pen` at x=12670, y=-220. Editable four-step Factory Process slide replica. Export: `.superpowers/sdd/replica-artifacts/exact-deck-12.png`. Report: `.superpowers/sdd/replica-exact-12-report.md`.
- **Exact Deck 8 replica (2026-08-20):** Added root frame `mtr8t` named `Exact - Deck 8 - SRE` on `Stack_Linear.pen` at x=12670, y=1000. Editable three-column Aiden for SRE slide replica with incident timeline, investigation pipeline, OCG / Temporal / OPA blocks, body copy, and SRE product lockup. Export: `.superpowers/sdd/replica-artifacts/exact-deck-8/mtr8t.png`. Report: `.superpowers/sdd/replica-exact-8-report.md`.
- **Exact Decks 1, 5, and 9 replicas (2026-08-20):** Added root frames `UdNdR` (`Exact - Deck 1 - Creation vs Ops`, x=12670 y=2200), `YMipV` (`Exact - Deck 5 - Factory Intent`, x=12670 y=3400), and `dkUBy` (`Exact - Deck 9 - Automation`, x=12670 y=4600) on `Stack_Linear.pen`. All are editable Pencil layer replicas, exported to `.superpowers/sdd/replica-artifacts/exact-deck-1/`, `exact-deck-5/`, and `exact-deck-9/`. Report: `.superpowers/sdd/replica-exact-1-5-9-report.md`.
- **Exact Decks 4 and 6 replicas (2026-08-20):** Added root frames `ekA26` (`Exact - Deck 4 - Domain Silos`, x=12670 y=9400) and `Hp0Tk` (`Exact - Deck 6 - Stateful Factory`, x=12670 y=10600) on `Stack_Linear.pen`. Both were created as `Up27k` / `trPMD`, exported blank, then fixed with the documented `Copy` to fresh id + `Delete` original workaround. Exports: `.superpowers/sdd/audit/r3-deck4/ekA26.png`, `.superpowers/sdd/audit/r3-deck6/Hp0Tk.png`. Report: `.superpowers/sdd/replica-exact-4-6-report.md`. Deck palette sampled from hires PNGs (shared by both slides): bg `#1B1811`, surface `#211D15`, text `#F1EAE0`, muted `#96897C`, purple `#BA99FD`, wire `#464646`. Colour sampler helper: `.superpowers/sdd/audit/sample_px.py` (pure-stdlib PNG decoder; hires sources are 8000x4500, so multiply 1024-space coordinates by 7.8125). Known gotcha: `ctx.bounds` and `problems` from `Get` are offset ~50px for same-session nodes and emit false "clipped" warnings; trust the exported PNG instead.
- Product mechanism upgrades in W2 should be rebuilt as editable Linear-token diagrams rather than text-only copy blocks; `bEaQH` now uses a six-step vertical investigation sequence for drift → alert → OCG root cause → remediation → policy-validated deploy → verification.
## Recent edits

- 2026-08-28: **Pencil MCP is workspace-only.** Official pen.dev docs (https://docs.pencil.dev/getting-started/installation#mcp-server, https://docs.pencil.dev/getting-started/ai-integration) auto-register a global Cursor server named `pencil` via the extension. This workspace does not use that. Pencil lives only in project `.cursor/mcp.json` as `pencil-docker` (Docker launcher + slugged socket `pencil-cursor_stackgen_website_redesign.sock`). `pencil-mcp-repair.sh` now skips `cursor.mcp.registerServer` so Cursor Tools & MCP does not show a global `extension-pencil`. Do not add Pencil to `~/.cursor/mcp.json`. After an extension update, re-run `PENCIL_PROJECT_SLUG=stackgen_website_redesign ~/.cursor/bin/pencil-mcp-repair.sh` and reload the window.

- 2026-08-27: **OpenMemory host lock + MCP sync.** This workspace's OpenMemory MCP is **only** GCE `openmemory-uswest` (`136.67.59.238:8771`, USER `StackgenWebsite`). Workspace `.cursor/mcp.json` SSE URL set to `http://136.67.59.238:8771/mcp/cursor/sse/StackgenWebsite`. Firewall `allow-openmemory-uswest` updated to include current egress `12.3.77.187/32` (kept prior `12.179.95.29/32`). Guide and MCP memories synced: hard rule stored on the GCP VM; global `~/.cursor/mcp.json` Zehra entry left untouched. Do not fail over to Coolify replica or Tailscale.
- 2026-08-29: **Task 1 CYfSl dark scaffold:** added `web/lib/cyfsl-frames.ts` with the dark-only CYfSl pencil ids, rewrote `web/app/globals.css` to a single dark `:root` token set using the extracted Pencil hexes, and simplified `web/app/layout.tsx` to font-only root markup with no theme providers or theme-init script. The smoke test in `web/__tests__/cyfsl-home.test.tsx` is intentionally red for Task 2 wiring.
- 2026-08-29: **Task 2 CYfSl home scaffold:** created the stub `web/components/cyfsl/` section set (`Nav` through `Footer`) plus `CyfslHome`, each wrapping the correct semantic element with `data-pencil-id` from `CYFSL_FRAMES`, and rewired `web/app/page.tsx` to render `CyfslHome`. Focused smoke test now passes.
- 2026-08-29: **Task 3 CYfSl content + shared primitives:** added `web/content/cyfsl.ts` with the requested eight-key content object from the canvas dump, created theme-free `CyfslLogo` and `CyfslPrimaryPill`, and minimally wired nav, hero, video, logos, and footer to consume that content while leaving `CyfslAssemblies`, `CyfslShell`, and `CyfslWhoItsFor` as stubs for later tasks. Added focused tests in `web/__tests__/cyfsl-content.test.ts` and `web/__tests__/cyfsl-home.test.tsx`; both pass.
- 2026-08-29: **Task 3 review fixes:** backfilled the missing `assemblies`, `shell`, and `whoItsFor` canvas strings in `web/content/cyfsl.ts`, added a `variant` prop to `CyfslPrimaryPill` for nav vs hero sizing, and locked the new content/styling with focused CYfSL tests. The Task 3 report now records the review-fix status and targeted test result.
- 2026-08-29: **Task 4 CyfslNav polish:** refined `web/components/cyfsl/CyfslNav.tsx` to the Pencil strip measurements (60px tall, 100px side padding, 26px link gap, 18px action gap) with the `CyfslLogo`, `cyfslContent.nav` links, and the `CyfslPrimaryPill` nav variant for the Book Demo CTA. The smoke test now explicitly keeps the nav CTA label in coverage while still asserting the page-level Schedule demo CTAs.
- 2026-08-29: **Task 5 CyfslHero layout:** centered `web/components/cyfsl/CyfslHero.tsx` to match the canvas hero stack, kept the hero primary CTA on `CyfslPrimaryPill`'s `hero` variant, converted the secondary CTA into an outline pill, and tightened `web/__tests__/cyfsl-home.test.tsx` to compare the rendered H1 against `cyfslContent.hero.heading` with whitespace stripped. Focused CYfSL smoke tests passed.
- 2026-08-29: **Task 7 CyfslAssemblies fallback:** replaced the `web/components/cyfsl/CyfslAssemblies.tsx` stub with a semantic section that uses shared CYfSL page padding, renders the `cyfslContent.assemblies.heading`, and embeds the exported `RBepL`, `F4Jlp`, and `hG9Ou` PNGs as full-width diagrams instead of re-implementing the dense canvas internals in React. `web/__tests__/cyfsl-home.test.tsx` now asserts the assemblies heading and all three diagram images, and the focused CYfSL test command passed (`7` files / `14` tests).

- 2026-08-21: **Task 6 blocked-manifest review fix:** `web/scripts/__tests__/clips.test.ts` now extracts the canonical `## Status` entry and requires both `.superpowers/sdd/factory-task-6-report.md` and `.superpowers/sdd/redaction-signoff.md` to have exactly one status value (`blocked`). The same suite now compares each artifact's uncleared-segment list directly against the `CLIPS`-derived 14-name manifest, rejecting extras and duplicates while preserving the existing duplicate/conflicting cleared-evidence guards. Verification passed with focused `scripts/__tests__/clips.test.ts` and `pnpm typecheck`. Commit: `2801f97`.
- 2026-08-21: **Wave 3 Task 10 Aiden OS port:** added `web/components/diagrams/AidenOsDiagram.tsx` from Figma node `1:8708` with an accessible single-SVG contract (`role="img"`, title, desc, panel ground, `DiagramText` width/maxLines, `data-part` hooks), downloaded the center-ring asset to `web/public/diagram-assets/aiden-os-center.svg`, changed `web/components/sections/platform/AidenOsLinks.tsx` to import the new component directly, and kept `web/components/diagrams/AidenOsLinksDiagram.tsx` as a thin compatibility wrapper. TDD evidence: new `AidenOsDiagram.test.tsx` failed first on the missing module, then focused diagram/section specs passed, followed by `pnpm typecheck`, full `pnpm test`, and `pnpm build`.
- 2026-08-21: **Task 11 review fixes:** `web/components/diagrams/product/AutomationMechanism.tsx`, `InfrastructureMechanism.tsx`, and `SreMechanism.tsx` now use source-derived cropped Figma viewBoxes instead of the hand-estimated `1240x820` layout, and `SreMechanism` renames the misleading `automation-ladder` hook to `investigation-ladder` while routing incident/policy body copy through bounded `DiagramText`. Added regression tests for source-derived viewBoxes, truthful SRE hook naming, wrapped-text bounds, and the final Infrastructure accessibility crop description. Verification passed with focused mechanism suites, `pnpm typecheck`, and `pnpm build`. Commits: `8d876ef`, `fbb212f`, `3573522`.
- 2026-08-21: **Task 10 left-frame orientation fix:** rotated `web/public/diagram-assets/aiden-os-left-side-frame.svg` into the actual portrait Figma `1:13675` geometry (`129.8968 x 551.0742`) instead of stretching a landscape asset, and updated `web/components/diagrams/AidenOsDiagram.tsx` to render that image with `preserveAspectRatio="xMidYMid meet"`. `web/components/diagrams/__tests__/AidenOsDiagram.test.tsx` now reads the committed SVG and asserts portrait intrinsic dimensions plus the Figma slot ratio, so a future landscape replacement fails even if the href stays the same. Verification passed: focused Aiden OS diagram/wrapper/section tests plus `pnpm typecheck`.
- 2026-08-21: **Task 5 review-fix provenance hardening:** `web/content/__tests__/governance.test.ts` now requires prompt provenance, industry provenance, healthcare proof hygiene, and explicit featured-case poster status rather than accepting bare strings. `web/lib/types.ts` now models `IndustryProvenance`, `ProductPrompt`, and `FeaturedCasePoster`; `web/content/industries.ts` swaps the healthcare placeholder-quote paraphrase for publishable Innovaccer case facts with source URL and marks financial-services proof with an approved-evidence marker; `web/content/home.ts` marks the greytHR poster as pending until a verified public asset exists; `product-automation` and `product-sre` now declare prompt provenance as unavailable rather than inventing transcript-backed input. TDD evidence: the tightened content tests failed first on four provenance gaps, then passed focused content tests + typecheck, followed by full `pnpm test` and `pnpm build`. Commit: `287c95d`.
- 2026-08-21: **Task 5 content model expansion:** `web/lib/types.ts` now adds `Industry` and `MomentumItem`; `web/content/industries.ts` constrains industry proof to financial services and healthcare only; `web/content/home.ts` now carries `useCases`, `momentum`, and a `featuredCase` reference for the greytHR interview; product content modules now expose copyable `prompt` strings, and `product-observability` adds the published greytHR testimonial with source URL. TDD evidence: governance tests were extended first, failed on missing `../industries`, then passed focused content tests, `pnpm typecheck`, full `pnpm test`, and `pnpm build`. Commit: `e392f47`.
- 2026-08-21: **Task 4 structural primitives:** added `web/components/primitives/NumberedSequence.tsx`, `StatBand.tsx`, `IridescentBand.tsx`, `Marquee.tsx`, and `PromptLine.tsx` with four new vitest files plus a `@keyframes marquee` entry in `web/app/globals.css`. TDD evidence: the new suite first failed with module-resolution errors for all four missing components, then passed focused `components/primitives` tests + `pnpm typecheck`, followed by full `pnpm test` and `pnpm build`. `PromptLine` uses a visible status label for copied state, `StatBand` preserves mechanism lines for every metric, and `Marquee` holds still under reduced motion through the shared hook.
- 2026-08-21: **Task 2 review hardening:** `web/components/motion/Reveal.tsx` now uses class-based opacity/translate transitions plus `motion-reduce` overrides so reduced-motion users get final-state content on first render without relying on render-time browser reads. `web/components/motion/useReducedMotion.ts` now guards missing `matchMedia` listener methods and falls back to legacy `addListener` / `removeListener` when present. `web/components/motion/__tests__/Reveal.test.tsx` now uses a controlled `IntersectionObserver` stub, checks hidden-before-observed separately, asserts the reduced-motion first-render override in server markup, and covers the partial-API fallback path.
- 2026-08-20: **Wave 1 T2 real motion foundation:** `web/components/motion/Reveal.tsx` now wraps children in a client-side `<div>` that reveals once via `IntersectionObserver` using `opacity` + `translateY(16px)` over 600ms with the task easing and optional delay, while `web/components/motion/useReducedMotion.ts` reads `prefers-reduced-motion` and safely falls back when browser motion APIs are unavailable in tests or non-observer environments. `MotionProvider` remains a no-op wrapper so existing call sites stay unchanged. Verification passed: focused motion vitest, full typecheck, full vitest, and production build.
- 2026-08-20: **Wave 0 Task 1 font asset correction:** replaced the three byte-identical local Geist binaries in `web/public/fonts/` with the official distinct static WOFF2 release assets from Vercel Geist v1.7.2 (`Regular` 400, `Medium` 500, `SemiBold` 600). `web/app/layout.tsx` already mapped those filenames to the correct fixed weights, so no code change was required. Verification passed: token test, typecheck, full vitest, and production build.
- 2026-08-20: **Wave 0 Task 1 token/font contract:** `web/app/globals.css` now defines the light factory-anchored semantic token set (`bg-base`, `panel`, `action`, `focus`, paired pass/halt roles, panel-aware focus ring) and replaces the blanket reduced-motion duration clamp with a scroll-behavior-only fallback. `web/app/layout.tsx` now loads local Geist through `next/font/local` on `--font-geist` while preserving the undeclared `--font-haffer` fallback position in `--font-sans`; `web/lib/__tests__/tokens.test.ts` locks the token names plus contrast contract (9 tests passing).
- 2026-08-20: **Responsive parity repair pass:** removed the global `1440px`
  body minimum, added mobile/tablet spacing and a native `<details>` navigation
  menu, restored visible StackGen wordmarks at narrow widths, stacked shared
  fixed-width headers and ChangeSurface panels, wrapped metric/logo/integration/
  case grids, and extended `web/e2e/diagnose.mjs` to probe desktop, tablet, and
  mobile widths. Final checks: 51 Vitest files / 171 tests pass, typecheck and
  production build pass, Impeccable detector returns `[]`, and all 30 route-width
  probes report no viewport overflow, SVG overlap, SVG overflow, or broken logos.
- 2026-08-20: **Canvas parity repair pass** on `main` (post Wave 1 merge `2bad884`), driven by user reports of missing logos and overlapping text. Investigation written to `docs/superpowers/wave1-parity-gap-analysis.md` before any edit; five root causes (wrong audit node map, unwrapped SVG text, dark-on-dark logos, uncropped mechanism heading band, section chrome inflation). User decisions: canvas text wordmarks for logos, DOM header wins over SVG header, fix P0 + P1 layout parity, all 10 routes. Results: overlapping text 13 pairs → **0**, frame overflow 8 → **0**, invisible logos → **0**, axe violations **0/11 routes**, unit tests **171 pass**. Height deltas vs canvas went from +288…+656 to: cases +3/+15/+28, products +109…+127, platform +188. Home Integrations was rebuilt to the full seven-category grid, which exposed a real +855 on home (the old 8-icon strip had been masking the diagram sections' duplicated heading band) — home diagram viewBox cropping was still in progress at the end of the session.

- 2026-08-19: Task 4 inserted Home `Factory Process` (`pnlIy`) between `Problem` and `ADF Loop`, using `Section Header Split` plus four exact process steps with hairline connectors. Exported screenshot: `.superpowers/sdd/task-4-artifacts/pnlIy.png`. Verification kept Home order Problem → Factory Process → ADF Loop and flagged that `t5DPzG` still has the four stage names but does not explicitly include `Aiden for Automation` text inside the loop copy.
- 2026-08-19: Task 6 rebuilt Product `bEaQH` Mechanism into an editable six-step investigation sequence with side tags `Infrawatch` → `ChangeCorrelation` → `RootCause` → `Remediation` → `Deploy` → `Verification`, plus the orchestration/policy footnote. Exported screenshot: `.superpowers/sdd/task-6-artifacts/k3S5j.png`. Banned scan on `bEaQH` returned 0 matches for `Aiden for DevOps`, `Olly`, `$`, `—`, and `–`.
- 2026-08-19: Task 7 rebuilt Product `zTOam` Mechanism as an editable six-step automation pipeline with two callout cards (`Active Gating`, `Self-Verification`) and step tags `Commit` → `BuildTest` → `OCGCheck` → `Gate` → `Deploy` → `Monitor`. Exported screenshot: `.superpowers/sdd/task-7-artifacts/u2Jbr.png`. Banned scan on `zTOam` returned 0 matches for `Aiden for DevOps`, `Olly`, `$`, `—`, and `–`; the hero title still contains `Aiden for Automation`.
- 2026-08-19: Task 8 rebuilt Product `T4FJtW` Mechanism by reusing the editable automation shell for an infrastructure-specific flow: `Intent` → `FactorySpec` → `Plan` → `Apply` → `Watch` → `Rollback`, with callout cards for `Intent input` and `Policy boundary`, plus an `Early Access` strip titled `Policy-bounded migration` placed between the mechanism and final CTA. Exported screenshot: `.superpowers/sdd/task-8-artifacts/T4FJtW.png`. Banned scan on `T4FJtW` returned 0 matches for `InfraOps`, `Aiden for DevOps`, dollar claims, `6→9`, `—`, and `–`.
- 2026-08-19: Task 9 rebuilt Product `OAfMk` Mechanism as a lighter editable OCG correlation diagram: `Metrics / Logs / Traces` links to `Infra state` and `Change history` through a central correlated-insight node, with prompt and handoff strips to show natural-language observability feeding SRE. Exported screenshot: `.superpowers/sdd/task-9-artifacts/y3RwQr.png`. Banned scan on `OAfMk` returned 0 matches for `Aiden for DevOps`, `Olly`, `$`, `—`, and `–`.
- 2026-08-19: Task 10 inserted the full Platform Operational Context Graph on `Platform` (`HL34b`) between `Two Planes` and `Product Links`, using a centered `OCG Shared Intel` hub with five connected satellite cards: Infrastructure Topology, Change Attribution, Drift History, Incident Causality, and Observability Correlations. Exported screenshot: `.superpowers/sdd/task-10-artifacts/HL34b.png`. Banned scan on the OCG section returned 0 matches for `Aiden for DevOps`, `Aiden for InfraOps`, `Olly`, `PNG`, `cyan`, `glow`, `—`, and `–`; Impeccable detector returned `[]`.
- 2026-08-19: Task 11 updated `Platform` (`HL34b`) after the OCG section with an `Aiden OS` band covering `Workflow orchestration` and `Financial governance`, plus a labeled `Roadmap` panel and the preserved `Four surfaces. One OS.` links above the existing final CTA. Exported screenshot: `.superpowers/sdd/task-11-artifacts/HL34b-final/HL34b.png`. Banned scan on `HL34b` returned 0 matches for `Aiden for DevOps`, `Aiden for InfraOps`, `Olly`, `glow`, `—`, and `–`; the final CTA ref still points at `xcXXD`. 
- 2026-08-19: Task 12 verified postn-deck incorporation on `JLg8h`, `A38GWG`, `T4FJtW`, `zTOam`, `OAfMk`, `bEaQH`, and `HL34b`. Pencil Get/Print showed 0 hard-fail hits for `Aiden for DevOps`, `Aiden for InfraOps`, `Olly`, slide-14 ROI strings (`$4M`, `$6M`, `$2.9M`), and em dashes in the target frames; Home keeps distinct `Factory Process` (`pnlIy`) and `ADF Loop` (`t5DPzG`), Home/mobile OCG teaser ids are `RBIMd` / `EuDQL`, Platform full OCG is `O4fic7`, and primary CTA text still resolves to `Schedule demo`. Report saved to `.superpowers/sdd/task-12-report.md`.
- 2026-08-19: Task 5 rebuilt `StackGen Home — Mobile` as `A38GWG` after the prior mobile frame was deleted, restoring Nav, Hero, Mechanism, Logos, Problem, Factory Process, ADF Loop, Agentic OS, Operational Context Graph teaser, Final CTA, and a mobile-adapted `Footer Simple` ref. Placeholder cleared on both `A38GWG` and desktop Home `JLg8h`; banned scan on mobile returned no `Olly`, `estate`, or em-dash matches.
 - 2026-08-19: Task 3 enriched Home Problem `vU48B` with deck-aligned paradox copy, an editable `Creation Ops Diagram` (`OVyjM`), and three compact failure rows (`ECPKp`, `G0xlsM`, `E0cYp`). Placeholder left true on `JLg8h` because Home Tasks 4-5 still follow.
 - 2026-08-19: Patched `docs/superpowers/specs/2026-08-19-site-ia-page-briefs.md` (Task 2) to add explicit diagram slots for Home / Product / Platform, add copy checklist items ("No slide-14 dollar ROI", "Factory process ≠ product loop"), and record Home section order: Problem → Factory Process → ADF Loop. Agent: coding assistant.
- **Impeccable polish (Home):** Mechanism moved under Hero; H1 64 + L1/L0 split; ADF stages Build/Govern/Observe/Remediate; logos restored; mobile mirrored. Linear world preserved (Persuade refinement).
- Customer logo + quote pack: `docs/proof/customer-logos-and-quotes.md` (Firecrawl from stackgen.com; assets in `.firecrawl/logos-quotes/assets/`). Only greytHR quote is published; all other quotes are PLACEHOLDER for canvas.
- One OpenMemory Coolify service per workspace (`openmemory-<slug>`), unique host ports, shared OpenAI env_file from zehra stack path.
- Prefer **project** `.cursor/mcp.json` for workspace-dedicated OpenMemory; keep global `~/.cursor/mcp.json` on Zehra unless intentionally changed.
- Prefer project-scoped remote MCP entries in `.cursor/mcp.json` when a service supports OAuth, keeping credentials out of the repository.
- Remote OAuth MCPs (Webflow, Sybill): use `npx -y mcp-remote <url>` in project `.cursor/mcp.json`; OAuth token cached in `~/.mcp-auth`; first use opens browser sign-in.
- Figma MCP (official remote): use native `"url": "https://mcp.figma.com/mcp"` in project `.cursor/mcp.json` — Figma MCP Catalog client only; OAuth via Cursor Connect (not mcp-remote CLI).
- Coolify list status for these stacks often shows `running:unknown` / stale `exited`; verify with TCP + SSE probe on the MCP port.
- Pencil MCP only for `.pen` — never Read/Grep encrypted pens.
- **Pencil MCP is workspace-only (2026-08-28):** Official pen.dev docs auto-register a global Cursor server named `pencil`. This workspace disables that (`__pencilSkipGlobalMcpRegister` in `pencil-mcp-repair.sh`). The only Pencil MCP is project `.cursor/mcp.json` `pencil-docker` (`PENCIL_PROJECT_SLUG=stackgen_website_redesign`). Never add Pencil to `~/.cursor/mcp.json`. After a Pencil extension update, re-run the repair script and reload the window. A `.pen` file must be open in the Pencil canvas editor or tools return “file needs to be open.”
- **Next.js Wave 1 foundation (T1, 2026-08-20):** `web/` on branch `wave1-nextjs`. Stack: Next 16.3, React 19.2, Tailwind v4 `@theme` tokens from `Stack_Linear.pen`, motion 13.1.0 (unused), Vitest+RTL. Shared types: `Metric`, `Quote`, `CustomerLogo`, `Cta`, `NavItem`, `DiagramProps`, `SectionProps<T>`. Nav: Product/Platform/Case Studies/Company + PRIMARY_CTA Schedule demo. Customer logos copied to `web/public/logos/customers/` (12 files). Docker compose profiles `dev`/`prod`. Known gaps: `postcss.config.mjs` not in T1 file ownership (Tailwind `@theme` build warning); pnpm standalone missing `@swc/helpers` symlink at runtime.
- **2026-08-19 content reset (option C):** Deleted all Scope A marketing-core page frames beyond Home/Mobile. Removed `docs/content-inventory/` and `.firecrawl/scope-a/`. Foundation kept: Direction Contract, Home, Mobile, component shelf. Old plan `docs/superpowers/plans/2026-08-19-stackgen-marketing-core-canvas.md` is **superseded** until a new content process + plan is approved.
