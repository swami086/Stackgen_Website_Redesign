// components/diagrams/product/InfrastructureMechanism.tsx — STUB
import type { DiagramProps } from '@/lib/types';

export function InfrastructureMechanism({ className, titleId = 'infrastructure-mechanism-title' }: DiagramProps) {
  return (
    <svg
      viewBox="0 0 1240 1275"
      className={className}
      role="img"
      aria-labelledby={titleId}
      data-stub="InfrastructureMechanism"
    >
      <title id={titleId}>Infrastructure mechanism diagram</title>
    </svg>
  );
}
