// components/diagrams/FactoryProcessDiagram.tsx — STUB
import type { DiagramProps } from '@/lib/types';

export function FactoryProcessDiagram({ className, titleId = 'factory-process-diagram-title' }: DiagramProps) {
  return (
    <svg
      viewBox="0 0 1440 604"
      className={className}
      role="img"
      aria-labelledby={titleId}
      data-stub="FactoryProcessDiagram"
    >
      <title id={titleId}>Factory process diagram</title>
    </svg>
  );
}
