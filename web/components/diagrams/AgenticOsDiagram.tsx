// components/diagrams/AgenticOsDiagram.tsx — STUB
import type { DiagramProps } from '@/lib/types';

export function AgenticOsDiagram({ className, titleId = 'agentic-os-diagram-title' }: DiagramProps) {
  return (
    <svg
      viewBox="0 0 1440 964"
      className={className}
      role="img"
      aria-labelledby={titleId}
      data-stub="AgenticOsDiagram"
    >
      <title id={titleId}>Agentic OS diagram</title>
    </svg>
  );
}
