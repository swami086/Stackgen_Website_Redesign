# Copy to stack/.env — never commit stack/.env
POSTGRES_PASSWORD=change-me-long-random
PAYLOAD_SECRET=change-me-64-hex-chars-minimum
# Browser-facing URL for Payload admin CSRF (host port 3002 when using this compose file)
PAYLOAD_PUBLIC_SERVER_URL=http://127.0.0.1:3002
